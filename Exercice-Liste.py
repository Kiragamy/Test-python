# -*-coding:Utf-8 -*
# print("hello")
from random import *
# Réécrivez la fonction list_aleat() ci-dessus, en utilisant la méthode append() pour construire la liste petit à petit à partir
# d'une liste vide (au lieu de remplacer les zéros d'une liste préexistante comme nous l'avons fait).

# s = []
# tailleListe=9
# for i in range(tailleListe):
#     s.append(random() )
# print(s)
# texte ="les saucisses et saucissons secs sont dans le saloir"
# lettres ={}
# for c in texte:
#         lettres[c] = lettres.get(c, 0) + 1
# print(lettres)

#Creer une liste de tuple
# lettres_triees=[]
# for key, value in lettres.items():
#     lettres_triees.append(key_value))
#
# print(lettres_triees)
# lettres_triees.sort()
# print(lettres_triees)

#
# def renvoi_list(liste_utilisateur):
#     # liste_renvoyee = []
#     # for mot in liste_utilisateur:
#     #     if mot.startswith("a"):
#     #         liste_renvoyee.append(mot)
#
#     return [mot.upper() for mot in liste_utilisateur if mot.startswith('a')]
#
#
# b = ["alexis", "tiffany", "amour", "forever"]
# print(renvoi_list(b))

# def renvoi_nombre(n, modulo):
#     return [number for number in range(n) if number % modulo == 0]
#
# print(renvoi_nombre(200, 11))

# def renvoi_voyelle(mot):
#     return [lettre for lettre in mot if lettre in ["a", "e", "i", "o", "u", "y"]]
#
# print(renvoi_voyelle("abraraccourcix"))

